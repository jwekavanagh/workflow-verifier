# Example: run the published commercial workflow-verifier CLI against this repo's examples/
# with a real WORKFLOW_VERIFIER_API_KEY and a reachable license API (COMMERCIAL_LICENSE_API_BASE_URL).
#
# Prerequisites:
# - GitHub secret WORKFLOW_VERIFIER_API_KEY from your product account
# - License server must accept POST /api/v1/usage/reserve for intent=verify
#
# Copy to .github/workflows/ in a repo that vendors the same examples/ layout, or run from this monorepo.
#
# Ambient discovery (job summary + failure PR comment): normative contract in docs/ambient-ci-distribution.md
name: AgentSkeptic (commercial CLI)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "examples/**"
  push:
    paths:
      - "examples/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      WFV_REPO_ROOT: ${{ github.workspace }}/node_modules/workflow-verifier
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Create examples/demo.db from seed.sql
        run: |
          node --input-type=module -e "
          import { readFileSync } from 'node:fs';
          import { DatabaseSync } from 'node:sqlite';
          const d = new DatabaseSync('examples/demo.db');
          d.exec(readFileSync('examples/seed.sql', 'utf8'));
          d.close();
          "

      - name: Install published workflow-verifier (commercial)
        run: npm install workflow-verifier@latest

      - name: Run contract verification
        id: wfv_verify
        continue-on-error: true
        env:
          WORKFLOW_VERIFIER_API_KEY: ${{ secrets.WORKFLOW_VERIFIER_API_KEY }}
        run: |
          npx workflow-verifier --workflow-id wf_complete \
            --events examples/events.ndjson \
            --registry examples/tools.json \
            --db examples/demo.db \
            > verify.stdout 2> verify.stderr

      - name: Append AgentSkeptic discovery summary
        if: always()
        run: |
          node "${WFV_REPO_ROOT}/scripts/render-discovery-ci.mjs" summary >> "$GITHUB_STEP_SUMMARY"

      - name: Render PR comment body on verification failure
        if: github.event_name == 'pull_request' && steps.wfv_verify.outcome == 'failure'
        run: |
          node "${WFV_REPO_ROOT}/scripts/render-discovery-ci.mjs" pr_body \
            --stderr-file verify.stderr \
            --workflow-stdout-file verify.stdout \
            > pr-comment-body.md

      - name: Upsert PR comment on verification failure
        if: github.event_name == 'pull_request' && steps.wfv_verify.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- workflow-verifier-discovery:v1 -->';
            const body = fs.readFileSync('pr-comment-body.md', 'utf8');
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });
            const withMarker = comments.filter((c) => (c.body || '').includes(marker));
            const existing = withMarker.length ? withMarker[withMarker.length - 1] : null;
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

      - name: Fail job when verification failed
        if: steps.wfv_verify.outcome == 'failure'
        run: |
          echo "::error::workflow-verifier verify failed; see step logs and PR comment (if pull_request)."
          exit 1
